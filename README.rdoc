= Overview Wikipage

Overview Wikipage is a Redmine plugin to show a wikipage in the project overview page.

= Install

1. copy the plugin to Redmine plugins folder
2. migrate database by a following command
  $ bundle exec rake redmine:plugins:migrate RAILS_ENV=production
3. restart Redmine
